# Django settings
DEBUG=False
SECRET_KEY=django-insecure-i+$7q*cy+1!jvjty71zpo1ecf$v!x6zqo(m1qf46@veozs97-g
ALLOWED_HOSTS=194.15.46.70,your-domain.com
CSRF_TRUSTED_ORIGINS=https://your-domain.com,http://194.15.46.70

# Database settings
DB_ENGINE=django.db.backends.postgresql
DB_NAME=cryptoobmen
DB_USER=postgres
DB_PASSWORD=postgresdb
DB_HOST=postgres
DB_PORT=5432

# Redis settings
REDIS_HOST=redis
REDIS_PORT=6379

# Email settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=cryptooveron@gmail.com
EMAIL_HOST_PASSWORD=IToveron!

# Security settings
SECURE_SSL_REDIRECT=False  # Установить True после настройки SSL
SESSION_COOKIE_SECURE=False  # Установить True после настройки SSL
CSRF_COOKIE_SECURE=False  # Установить True после настройки SSL
SECURE_HSTS_SECONDS=0  # Установить на 31536000 (1 год) после настройки SSL
SECURE_HSTS_INCLUDE_SUBDOMAINS=False  # Установить True после настройки SSL
SECURE_HSTS_PRELOAD=False  # Установить True после настройки SSL 